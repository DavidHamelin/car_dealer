{% extends 'base.html.twig' %}

{% block title %}TypeOfSale index{% endblock %}

{% block body %}
    <h1>TypeOfSale index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_of_sale in type_of_sales %}
            <tr>
                <td>{{ type_of_sale.id }}</td>
                <td>{{ type_of_sale.name }}</td>
                <td>
                    <a href="{{ path('app_type_of_sale_show', {'id': type_of_sale.id}) }}">show</a>
                    <a href="{{ path('app_type_of_sale_edit', {'id': type_of_sale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_of_sale_new') }}">Create new</a>
{% endblock %}
