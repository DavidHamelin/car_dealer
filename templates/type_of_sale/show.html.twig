{% extends 'base.html.twig' %}

{% block title %}TypeOfSale{% endblock %}

{% block body %}
    <h1>TypeOfSale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_sale.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_sale.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_sale_index') }}">back to list</a>

    <a href="{{ path('app_type_of_sale_edit', {'id': type_of_sale.id}) }}">edit</a>

    {{ include('type_of_sale/_delete_form.html.twig') }}
{% endblock %}
